# URL base da API do backend (Spring Boot).
# Em desenvolvimento local:
VITE_API_URL=http://localhost:8080/api

# Em producao (Netlify), configure esta variavel apontando para o backend no Fly.io, ex:
# VITE_API_URL=https://controle-entrada-caema-api.fly.dev/api
